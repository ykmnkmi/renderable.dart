name: renderable
description: An simple (and not yet powerful) template language for Dart.
version: 0.0.1

environment:
  sdk: '>=2.7.1 <3.0.0'

dependencies:
  petitparser: ^3.0.1
  string_scanner: ^1.0.5

dev_dependencies:
  pedantic: ^1.9.0