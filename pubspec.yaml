name: renderable
description: An simple (and not yet powerful) template language for Dart like Jinja or Stencil.
version: 0.0.6-dev

environment:
  sdk: '>=2.9.2 <3.0.0'

dependencies:
  build: any
  meta: any
  path: any
  source_gen: any
  string_scanner: any

dev_dependencies:
  build_runner: any
  dart_style: any
  pedantic: any
  test: any
