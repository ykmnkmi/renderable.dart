name: renderable
description: An simple (and not yet powerful) template language for Dart like Jinja or Stencil. 
version: 0.0.2+1

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  petitparser: ^3.0.1

dev_dependencies:
  # for comparsion and testing
  angular_ast: ^0.5.11
  pedantic: ^1.9.0
